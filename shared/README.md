# Shared

Shared utilities and configuration used across the backend and frontend.

## Structure
- `config/`: Centralized configuration (environment variables, secrets management, app settings).
- `logging/`: Logging utilities or adapters that can be reused across services.

Add additional shared modules here as the project grows (e.g., validation schemas, analytics adapters).
